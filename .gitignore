# Dependencies
node_modules/

# Build outputs
# Note: In production Obsidian plugins, main.js is committed to release branches, 
# but for source code repositories it is usually ignored and built via CI.
main.js

# Local environment / secrets
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS-specific files
.DS_Store
Thumbs.db

# Obsidian local testing / caches
.obsidian/
sync-queue.json
sync-metadata.json
